New Post "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

You will be notified of any new comments on your post.

<%= @post.body %>

Visit online at <%= topic_post_url(@post.topic, @post) %>
